# Motorcycle Dealer

Things to use

| Tehnologie       | stuff                                                                   |
| ---------------- | ----------------------------------------------------------------------- |
| PostgreSQL + JPA | relații între entități, lazy/eager loading, indexuri, tranzacții        |
| MongoDB          | când SQL vs NoSQL, documente vs relații, aggregation pipeline           |
| Redis            | caching strategies (TTL, invalidare), diferența față de DB              |
| RabbitMQ         | sync vs async, ce e o coadă, retry logic, dead letter queue             |
| WebSocket        | diferența față de HTTP, când îl folosești, polling vs push              |
| Docker Compose   | ce e un container vs VM, networking între servicii, volumes             |
| Nginx            | ce e un reverse proxy, load balancing, de ce nu expui direct backend-ul |
| JWT              | stateless auth, ce conține un token, refresh token vs access token      |

